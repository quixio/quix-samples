# Build-only test for docker/influxdb3.
# Verifies the image builds and the init script is in place.

services:
  influxdb3:
    build:
      context: ../../../docker/influxdb3
      dockerfile: dockerfile
    entrypoint: >
      sh -c "
        test -x /init.sh &&
        echo 'Success: build + layout check passed'
      "
    stop_grace_period: 3s
